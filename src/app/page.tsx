import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Nirbhik: Aid For Every Need',
};

export default function HomePage() {
  return <HomeView />;
}
