import ProjectCreateView from 'src/sections/projects/add/user-create-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new job',
};

export default function JobCreatePage() {
  return <ProjectCreateView />;
}
